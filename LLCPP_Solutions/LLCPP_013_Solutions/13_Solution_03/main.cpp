
#include <iostream>
#include <string>
using namespace std;

enum class Vegetables {
	Celery,
	Carrot,
	Lettace,
	Garbage,
	Fingers,
	Car
};

enum class Bread {
	Wheat,
	Cinderblock,
	Brick,
	EnrichedUranium
};

int main() {

	string y;
	getline(cin, y);
	return 0;
}